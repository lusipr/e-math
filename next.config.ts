import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['img.youtube.com', 'img.freepik.com', 'lh3.googleusercontent.com'],

  },
};

export default nextConfig;
